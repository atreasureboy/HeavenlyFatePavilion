# 天机阁架构设计对话路线图

状态：Accepted

记录日期：2026-07-24

## 一、这份路线图解决什么

天机阁采用“全域骨架先行，局部深度迭代”，不从 MVP 逐步扩建。因此，设计工作不能按照“先数据库、再服务、再 Agent”的组件顺序推进。

正确顺序是：

```text
目标与边界
→ 用户与质量属性
→ 业务能力和数据生命周期
→ 领域语义和状态所有权
→ 治理、Agent 与 Tool
→ 多租户、安全和策略
→ 运行、数据与分布式系统
→ 成本、容量、可靠性
→ 纵向场景验证
→ 架构收敛与面试呈现
```

用户不需要预先知道全部问题。每个阶段由 Codex 担任架构访谈官：

1. 阅读现有材料；
2. 每轮只提出少量关键问题；
3. 根据回答指出隐含假设和矛盾；
4. 给出两个或多个候选方案及取舍；
5. 由用户作出裁定或保留开放；
6. 将结论写入思路、迭代、ADR 或当前架构；
7. 检查本阶段完成门槛；
8. 达标后进入下一阶段。

## 二、用户如何与 Codex 对话

### 推荐方式

不要一次问“帮我把整个天机阁设计完”。每次使用以下结构：

```text
我们现在进行第 N 阶段。
请你先读取已有架构材料，以架构访谈官身份：
1. 一次只问我 3～5 个最关键的问题；
2. 根据我的回答追问，不要急着定方案；
3. 主动指出冲突、遗漏和错误假设；
4. 给出候选方案、代价和你的建议；
5. 将有价值的内容按成熟度归档；
6. 达到本阶段完成门槛后告诉我。
```

### 每轮回答不必专业

用户可以用直觉、比喻、例子或“不确定”回答。Codex 负责将其转换为：

- 正式术语；
- 架构驱动因素；
- 边界；
- 不变量；
- 候选方案；
- 开放问题；
- ADR；
- 验证场景。

### 三种允许的结论

每个问题不必立刻二选一，可以产生：

- `Accepted`：当前明确采纳；
- `Proposed`：候选方案，等待验证；
- `Open`：信息不足，保留问题和验证方法。

## 三、总体阶段地图

`Skeleton v1.0` 已经完成阶段 1，并在骨架级提前覆盖了原路线图中阶段 2～13 的宏观位置。后续不再机械按旧编号逐段推进，而按依赖关系提高专题分辨率。

当前深化顺序调整为：

```text
P1 情报语义与核心对象
→ P2 契约、状态与纠正传播
→ P3 数据、索引与知识供应
→ P4 Agent / Tool 治理与 Token 经济性
→ P5 多租户、身份、权限与策略
→ P6 智能执行与分布式运行
→ P7 质量、证据、审核与事实裁定
→ P8 容量、成本、可靠性与灾备
→ P9 六部和公共官署专题
→ P10 纵向场景、对抗验证与面试呈现
```

选择 P1 作为首个专题，是因为其他专题都会引用 Fact、Entity、Change、Assessment、Risk 和 Product 的正式语义。用户原型与现实价值不作为前置阶段，因为本项目的交付物是架构本身，不以当前落地客户或商业实现为目标；租户差异将在 P5 和跨专题场景中继续约束设计。

| 阶段 | 主题 | 主要成果 |
|---|---|---|
| 0 | 设计工作协议 | 迭代、记录、推送和成熟度规则 |
| 1 | 整体架构骨架 | 根节点、一级架构面、二级能力域和横切维度 |
| 2 | 用户、租户与质量属性 | 用户原型、需求模型、成功与失败标准 |
| 3 | 业务能力与数据生命周期 | 能力树、收集到管理的业务主链 |
| 4 | 领域语义与状态所有权 | 核心术语、领域边界、权威数据所有者 |
| 5 | 世界观与治理模型 | 仙人、皇帝、三省六部和公共官署 |
| 6 | Agent / Tool 与 Token 经济性 | Agent 放置、升级策略和成本不变量 |
| 7 | 多租户、身份、安全与合规 | 隔离、授权、策略层级和数据边界 |
| 8 | 智能执行与分布式运行 | Agent、Run、Task、队列、状态机和故障恢复 |
| 9 | 数据与存储架构 | 事实、原件、搜索、图谱、向量和时序 |
| 10 | 质量、证据与验证 | Claim、Evidence、Fact 和门下验证 |
| 11 | 容量、成本与可靠性 | 定量模型、SLO、灾备和降级 |
| 12 | 纵向场景与对抗验证 | 端到端正常、异常和攻击场景 |
| 13 | 架构收敛与 ADR | 消除矛盾，形成当前生效架构 |
| 14 | 面试呈现 | 概要、图集、讲解路线和追问入口 |

阶段不是严格单向的。后续验证发现问题时，可以回到前一阶段，但必须留下迭代和决策记录。

## 四、阶段 0：确认工作协议

### 当前状态

基本完成。

已有：

- 思路、迭代和当前架构分层；
- 文档状态；
- 编号规则；
- 对话自动沉淀 Skill；
- 大规模修改自动推送 GitHub；
- “全域骨架先行，局部深度迭代”方法。

### 还需要问

```text
请检查天机阁现有文档治理和迭代协议。
哪些规则仍不足以支撑多年、百万字规模的架构设计？
请只指出会造成版本混乱、概念冲突或无法追溯的问题。
```

### 完成门槛

- 所有材料都有明确归属；
- 新旧方案不会互相覆盖；
- 重大决定可以追溯；
- 当前有效架构具有唯一入口；
- 大规模修改可版本化。

## 五、阶段 1：整体架构骨架

这是当前正式架构设计的第一步。目标不是深入任何局部，而是为整个终局架构建立稳定坐标系。

必须先确定：

- 根节点和一级架构面；
- 每个一级架构面的二级能力域；
- 情报业务主干；
- 三省六部在整体中的位置；
- 天机阁自身智能运行能力与外部参考项目的关系；
- 多租户是树节点还是横切维度；
- 后续专题在骨架中的归属。

### 第一轮直接这样问

```text
我们开始天机阁架构设计第 1 阶段：整体架构骨架。
请你担任架构访谈官，一次问我 3～5 个问题。
先帮助我回答：
1. 天机阁最核心的业务主干是什么？
2. 三省六部是顶层、治理面还是业务领域？
3. 整体应该划分为哪些一级架构面？
4. 智能运行、数据底座、自我迭代和多租户在整体中处于什么位置？
不要深入安全、数据库、协议或单个官署内部。
```

### 必须回答的问题

- 业务主链；
- 用户和产品面；
- 情报生产面；
- 智能治理面；
- 运行与数据面；
- 平台控制面；
- 三省六部与业务能力的关系；
- 天机阁自身运行面的边界及对 AgentOS 等外部项目的参考关系；
- 多租户和其他横切维度。

### 产出

- 第一版整体根图；
- 一级架构面；
- 二级能力域；
- 顶层数据流和控制流；
- 横切维度；
- 局部专题索引；
- 骨架覆盖度检查。

### 完成门槛

阶段 1 不因第一轮内部闭合而结束。完成门槛是：

- 任何后续宏观问题都能挂入一个明确的一级面、二级节点、横切维度或外部边界；
- 从业务、数据、Agent、治理、多租户、生态、运行形态、时空规模、生存性和未来演进等视角扫描后，没有整片结构空白；
- 控制、数据、知识、授权、预算、审计和反馈流能够分别阅读；
- 新增结构反复经过对象、契约、状态、横切和场景闭合；
- 至少连续两轮宏观扫描不再改变一级骨架。

`Skeleton v0.1` 只代表当前已知结构第一轮内部闭合；达到 `Skeleton v1.0` 整体骨架基线后，才进入局部专题深度设计。详细计划见[阶段 1：整体骨架扩展与完善计划](./02-architecture-iterations/v0001-concept-baseline/phase-01-skeleton-expansion-plan.md)。

## 六、阶段 2：用户、租户与质量属性

### 开始时这样问

```text
我们进入第 2 阶段：用户、租户和质量属性。
请先识别可能的用户原型和租户类型，再逐个访谈我：
他们为什么使用天机阁、愿意付出什么成本、最害怕什么错误？
最后把需求转化为可测试的质量属性场景，不要停留在“高可用、高性能”口号。
```

### 必须讨论

- 个人研究者；
- 企业情报团队；
- 投资和商业分析团队；
- 安全或地缘分析团队；
- 平台管理员；
- 租户管理员；
- 普通查询者；
- 审核和合规人员；
- 外部协作者。

### 每类用户需要回答

- 任务目标；
- 使用频率；
- 数据敏感度；
- 新鲜度；
- 证据深度；
- 可接受延迟；
- 可接受成本；
- 风险容忍度；
- 输出形式；
- 是否允许自动行动。

### 产出

- 用户原型；
- 租户原型；
- 八维需求模型；
- 质量属性优先级；
- 质量属性场景；
- 套餐或服务等级的概念模型。

### 完成门槛

至少三个差异明显的用户场景能够推导出不同的调查、模型、审核和预算策略。

## 七、阶段 3：业务能力与数据生命周期

### 开始时这样问

```text
我们进入第 3 阶段：业务能力和数据生命周期。
请不要从官署或技术实现出发。
以“用户提出目标，到收到可信情报并产生反馈”为主线，
逐层拆解天机阁必须具备的业务能力。
重点检查收集、清洗、整理、分配、管理五段是否完整。
```

### 必须讨论

- 主动调查；
- 持续监测；
- 数据源发现和准入；
- 数据收集；
- 数据清洗；
- 实体和事实整理；
- 领域、存储、权限和产品分配；
- 数据质量和生命周期管理；
- 报告、告警和 API 产品；
- 用户反馈闭环。

### 产出

- 业务能力树；
- 价值流；
- 五段数据主链；
- 关键业务事件；
- 每段的输入、输出和权威所有者；
- 业务能力与质量属性矩阵。

### 完成门槛

任何一个用户产品都能追溯到数据来源、加工步骤、审核过程和管理责任。

## 八、阶段 4：领域语义与状态所有权

### 开始时这样问

```text
我们进入第 4 阶段：领域模型。
请从 Document、Evidence、Claim、Fact、Entity、Event、
Assessment 和 Intelligence Product 开始访谈我。
不要先选数据库；先消除术语歧义，并为每种核心状态确定唯一权威所有者。
```

### 必须讨论

- Source、Document、Snapshot；
- Evidence；
- Claim；
- Verified Fact；
- Disputed Claim；
- Entity、Identity 和 Alias；
- Event 和 Relationship；
- Signal、Inference、Assessment、Recommendation；
- Intelligence Product；
- 双时态；
- 公共事实和租户派生判断。

### 产出

- 统一语言；
- 领域概念图；
- 限界上下文；
- 聚合和状态所有权；
- 领域事件；
- 事实生命周期；
- 第一批数据不变量。

### 完成门槛

系统中不存在“事实”“记忆”“报告”“知识”等词在不同章节代表不同对象的情况。

## 九、阶段 5：世界观与治理模型

### 开始时这样问

```text
我们进入第 5 阶段：天机阁治理模型。
请把已经确定的业务能力和领域边界映射到仙人、皇帝、三省六部及公共官署。
每次映射都要求说明：为什么匹配、拥有什么权力、不能做什么、
与相邻机构如何制衡。不要为了历史名称强行映射。
```

### 必须讨论

- 仙人和人类主权；
- 皇帝的统筹权与权限上限；
- 中书省规划权；
- 尚书省执行统筹权；
- 门下省审核权；
- 六部领域边界；
- 通政司、国史馆、文渊阁、钦天监；
- 大理寺、都察院、翰林院和枢密院；
- 任免、授权、奏报、用印、封驳和问责。

### 产出

- 权力与职责矩阵；
- 机构边界；
- 决策权、执行权和审核权分离；
- 官制术语与正式技术语义映射；
- 职责冲突清单。

### 完成门槛

每个权力和核心状态只有一个最终责任主体，且任何高风险行为都不存在自我授权和自我审核。

## 十、阶段 6：Agent / Tool 与 Token 经济性

### 开始时这样问

```text
我们进入第 6 阶段：Agent、Tool 和 Token 经济性。
请逐个检查前面能力树中的节点：
它需要 Agent、Tool、规则、小模型、数据库、Worker 还是人工？
以不确定性、吞吐、风险、可复现性和成本为依据，不以官职高低直接决定。
最后形成升级和跳过策略。
```

### 必须讨论

- Agent 作为认知中断处理者；
- Agent 触发条件；
- 确定性优先顺序；
- 三省何时可以跳过；
- 部级 Agent 何时参与；
- 上下文最小化；
- Evidence 引用；
- 模型分层；
- 缓存和中间产物；
- 停止条件；
- Token 优化不降低功能的不变量；
- 人工升级。

### 产出

- Agent / Tool 决策矩阵；
- Agent Registry 概念；
- 升级状态机；
- Context Envelope；
- Budget Envelope；
- 模型路由策略；
- Token 成本模型；
- 质量补偿机制。

### 完成门槛

任何一次 Agent 调用都能回答“为什么不能由更便宜、更确定的组件完成”。

## 十一、阶段 7：多租户、身份、安全与合规

### 开始时这样问

```text
我们进入第 7 阶段：多租户、身份、安全与合规。
请从仙门、仙人、Court Profile 和一次任务的授权上下文开始，
逐步追问身份、数据隔离、委托、工具权限、私有覆盖层和跨租户协作。
请主动以攻击者视角寻找越权路径。
```

### 必须讨论

- 平台、租户和用户；
- 用户在多个租户中的身份；
- Court Profile；
- RBAC、ABAC、关系和能力授权；
- 权限委托与衰减；
- 公共、租户、用户和联合数据域；
- Tool 凭证；
- Prompt Injection；
- 数据投毒；
- DLP；
- 删除和派生删除；
- 地域、版权和来源许可；
- 跨租户会盟。

### 产出

- 身份模型；
- 租户隔离模型；
- 授权上下文；
- 策略层级；
- 数据分类；
- 威胁模型；
- 信任边界图；
- 高风险审批矩阵。

### 完成门槛

任意一次查询、Agent 调用、Tool 调用和数据访问都能说明主体、租户、授权来源、权限范围和审计记录。

## 十二、阶段 8：智能执行与分布式运行

### 开始时这样问

```text
我们进入第 8 阶段：天机阁智能执行和分布式运行。
请从一次调查 Run 开始，建立 Task、Agent、Tool、Worker、Queue、
Lease、Retry、Cancel 和 Event Log 的状态机。
重点追问崩溃、重复执行、超时、循环调用、死锁和人工取消。
AgentOS 只作为可参考的外部项目，不预设天机阁依赖其接口或实现。
```

### 必须讨论

- Run、Task、Session 和 Binding；
- Agent 模板、身份和运行实例；
- 同步调用、异步派发和消息；
- Workflow 与尚书省的边界；
- Scheduler 与钦天监的边界；
- 幂等；
- 租约；
- 重试；
- 超时和取消；
- 背压；
- Agent 调用深度；
- 循环和死锁；
- Event Log；
- 人类接管。

### 产出

- 运行时组件图；
- Run / Task 状态机；
- 调用和消息协议；
- 故障恢复表；
- 并发和一致性规则；
- Windows / OS 类比的正式映射。

### 完成门槛

对于任何节点崩溃，都能说明任务处于什么状态、由谁发现、是否重试、如何避免重复副作用。

## 十三、阶段 9：数据与存储架构

### 开始时这样问

```text
我们进入第 9 阶段：数据与存储架构。
请根据已经确定的领域状态和访问模式选择逻辑存储能力。
不要先报数据库产品名；先讨论事务、一致性、索引、查询、版本、分区和恢复需求。
```

### 必须讨论

- 原始资料；
- 结构化事实；
- Claim / Evidence 图；
- 全文检索；
- 实体关系；
- 向量相似性；
- 时间线和时序指标；
- Event Log；
- 缓存；
- 公共底座与租户覆盖层；
- 分区和热点；
- 备份、恢复和归档。

### 产出

- 逻辑数据架构；
- 数据所有权矩阵；
- 存储选择 ADR；
- 一致性模型；
- 索引策略；
- 数据血缘；
- 保留和删除传播。

### 完成门槛

每一种数据都有权威来源、读写主体、查询模式、一致性要求、生命周期和恢复策略。

## 十四、阶段 10：质量、证据与验证

### 开始时这样问

```text
我们进入第 10 阶段：可信、证据和验证。
请从“一个 Claim 如何成为 Verified Fact”开始访谈，
设计来源独立性、证据等级、反证、冲突保留、门下审核和人工复核。
要求所有质量概念可度量。
```

### 产出

- 证据等级；
- 来源信誉模型；
- Claim 生命周期；
- 验证策略；
- 冲突裁定流程；
- 质量指标；
- 黄金集和回归策略；
- 情报产品可信等级。

### 完成门槛

任何正式结论都可以说明证据、来源、生成者、审核者、置信度和争议状态。

## 十五、阶段 11：容量、成本与可靠性

### 开始时这样问

```text
我们进入第 11 阶段：容量、成本和可靠性。
请先要求我选择三组假设规模，再建立采集量、数据增长、查询量、
Agent 调用、Token、存储、队列和故障预算模型。
不要使用没有数字的“高并发”“海量数据”。
```

### 三组建议规模

- 小型：个人或小团队租户；
- 中型：企业情报团队；
- 终局：多租户公共情报平台。

### 产出

- 容量假设；
- SLO / SLI；
- Token 和基础设施成本；
- 户部预算模型；
- 限流、公平调度和背压；
- 降级矩阵；
- RPO / RTO；
- 多区域和灾备方案；
- 故障预算。

### 完成门槛

所有“可扩展、高可用、成本可控”都能够被数字、假设和失败条件解释。

## 十六、阶段 12：纵向场景与对抗验证

### 开始时这样问

```text
我们进入第 12 阶段：纵向场景验证。
请不要再新增横向组件。
选择一个场景，从用户意图一直追踪到数据、权限、预算、Agent、
Tool、审核、输出、审计和故障恢复。
每遇到无法回答的问题，就回写对应架构章节。
```

### 建议顺序

1. 同名人物证据级调查；
2. 企业持续监测和人员变动；
3. 高可信来源事实冲突；
4. Token 预算不足；
5. Prompt Injection；
6. 租户私有数据删除；
7. 突发流量洪峰；
8. 皇帝 Agent 异常并由人类接管。

### 产出

- SCN 场景文档；
- 正常时序图；
- 异常分支；
- 数据和权限流；
- 故障注入结果；
- 架构缺口清单；
- 修复后的 ADR。

### 完成门槛

至少八个场景能够闭环，且每个重要架构板块至少被一个正常场景和一个异常场景验证。

## 十七、阶段 13：架构收敛与当前版本

### 开始时这样问

```text
我们进入第 13 阶段：架构收敛。
请建立一份矛盾清单，检查术语、职责、状态所有权、权限、调度、
预算和审核是否在不同章节中冲突。
将已验证方案形成 ADR，并把生效结果提升到 current-architecture。
不要删除历史方案。
```

### 产出

- 矛盾和缺口审计；
- 已接受 ADR；
- 第一版当前生效架构；
- 被替代和退役材料；
- 架构不变量目录；
- 当前架构完整导航。

### 完成门槛

当前架构能够独立阅读，所有重要决定可追溯，历史材料不会与当前定义混淆。

## 十八、阶段 14：面试呈现

### 开始时这样问

```text
我们进入第 14 阶段：面试呈现。
请把完整架构压缩成 5 分钟、15 分钟、30 分钟和深度追问四套讲法。
不要牺牲准确性，但要突出问题、权衡、决策和验证，而不是组件数量。
```

### 产出

- 一页项目摘要；
- 10～20 页架构概要；
- 核心图集；
- 5 / 15 / 30 分钟讲解；
- 十个关键 ADR；
- 五个复杂故障故事；
- 常见质疑和回答；
- 深度文档索引。

### 完成门槛

面试官无需阅读全部卷宗即可理解项目价值，并且每次深入追问都能进入已有的严谨设计。

## 十九、我们现在处于哪里

当前已经完成或正在进行：

- 阶段 0：基本完成；
- 阶段 1：正在进行整体架构骨架访谈；作品目标和权力边界已有初稿，但细节延期；
- 阶段 3：已经形成五段数据主链候选；
- 阶段 5：已经形成三省六部初步映射；
- 阶段 6：已经形成 Agent / Tool 基础骨架；
- 其他阶段只有零散设想。

当前只推进整体骨架，暂不进入授权、安全、数据模型和运行机制等局部专题。

## 二十、用户下一句话

用户现在只需要发送：

```text
我们开始第 1 阶段：北极星、系统边界与非目标。
请你担任架构访谈官。
一次只问我 3～5 个最关键的问题，根据我的回答继续追问；
不要先讨论具体技术，也不要急着替我做决定。
当信息足够时，给出候选定义、冲突和建议，
把结果归档，但在我确认前不要提升为 Accepted。
```

此后每个阶段只需说“进入第 N 阶段”，Codex 应根据本路线图继续主持设计。
