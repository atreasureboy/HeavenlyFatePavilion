# 文档与迭代骨架

## 一、三类核心材料

天机阁的长期设计材料必须首先归入以下三类之一：

```text
Idea
尚未成为正式架构的观察、类比、假设和灵感
        │
        ▼
Iteration
某一轮架构推演的完整输入、变更、权衡和结果
        │
        ▼
Current Architecture
经过当前迭代采纳、此刻有效的架构定义
```

三者不能混写：

- 思路不因被记录就自动成为架构；
- 当前架构发生变化时，旧版本不能被直接抹除；
- 迭代文档记录“为什么变化”，当前架构记录“现在是什么”；
- 被否决的思路仍然保留，但必须注明状态和原因。

## 二、目录地图

| 目录 | 用途 | 内容是否代表当前决定 |
|---|---|---|
| `00-project-foundation` | 愿景、边界、原则、目标与质量属性 | 是，且变化应谨慎 |
| `01-idea-lab` | 原始设想、制度类比、假设和退役思路 | 否 |
| `02-architecture-iterations` | 每轮架构演进的完整记录 | 仅代表对应历史版本 |
| `03-current-architecture` | 当前生效的总体架构视图 | 是 |
| `04-domain-design` | 领域模型、边界上下文和业务不变量 | 以文档状态为准 |
| `05-scenarios-and-flows` | 用例、时序、异常和端到端验证场景 | 以文档状态为准 |
| `06-architecture-decisions` | ADR 与关键取舍 | 已接受 ADR 代表决定 |
| `07-analysis-and-validation` | 容量、安全、成本、一致性和故障分析 | 分析结论 |
| `08-reference-models` | 术语、协议、状态机和分类法 | 当前公共参考 |
| `09-presentation` | 面试摘要、图集和讲解路径 | 展示材料 |
| `99-archive` | 不再参与当前设计的历史材料 | 否 |

## 三、迭代规则

每轮重要迭代建立独立目录：

```text
02-architecture-iterations/
└── vNNNN-short-name/
    ├── README.md
    ├── inputs.md
    ├── proposed-changes.md
    ├── impact-analysis.md
    ├── open-questions.md
    └── outcome.md
```

迭代完成后：

1. 将被采纳的结果合并进 `03-current-architecture` 及相关专题；
2. 对重大选择建立或更新 ADR；
3. 更新术语、协议和状态模型；
4. 在 `outcome.md` 中记录采纳、拒绝和延期项目；
5. 保留整个迭代目录，不覆盖历史。

## 四、文档状态

正式文档应在开头声明一种状态：

- `Seed`：原始种子，尚未论证；
- `Draft`：正在设计；
- `Proposed`：等待架构评审；
- `Accepted`：当前采纳；
- `Superseded`：已被新设计替代；
- `Retired`：明确停止使用；
- `Reference`：只提供背景或词汇参考。

## 五、编号约定

- 架构迭代：`v0001`、`v0002`；
- ADR：`ADR-0001`、`ADR-0002`；
- 场景：`SCN-0001`；
- 威胁：`THR-0001`；
- 故障案例：`FLR-0001`；
- 架构不变量：`INV-0001`；
- 开放问题：`Q-0001`。

编号只表示稳定身份，不表示重要程度。

## 六、对话驱动的持续沉淀

天机阁架构讨论使用 `evolve-heavenly-fate-pavilion` Skill 维护。

只要一次对话产生了有价值的新思路、边界、原则、权衡、风险、场景、术语或决定，就必须在同一轮对话中将其写入本项目，而不能只保留在聊天记录中。

记录时遵循以下原则：

1. 先搜索已有内容，避免重复建立相同概念；
2. 区分原始思路、待验证假设、迭代候选和当前正式架构；
3. 不因一个想法听起来合理就直接将其标记为已采纳；
4. 用户和 Codex 在讨论中产生的实质性新见解都属于候选材料；
5. 重要决定应形成从思路、迭代、ADR 到当前架构的追踪链；
6. 在对话回复中简要说明本轮更新了哪些文件；
7. 不记录寒暄、简单重复及没有架构意义的措辞变化。

概念成熟过程如下：

```text
对话中的新见解
→ 思路或假设
→ 当前迭代输入
→ 方案与影响分析
→ 架构决策
→ 当前生效架构
→ 场景和专题分析验证
```

